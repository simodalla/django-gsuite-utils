============
Installation
============

At the command line::

    $ easy_install django-gsuite-utils

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-gsuite-utils
    $ pip install django-gsuite-utils
